import { DisableContorlDirective } from './disable-contorl.directive';

describe('DisableContorlDirective', () => {
  it('should create an instance', () => {
   // const directive = new DisableContorlDirective();
    //expect(directive).toBeTruthy();
  });
});
